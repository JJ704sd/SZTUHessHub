# Keep the server and browser environment values identical. Missing values fail safe to development.
HSEEHUB_ENV=development
NEXT_PUBLIC_HSEEHUB_ENV=development
NEXT_PUBLIC_SITE_URL=http://localhost:3000
HSEEHUB_RELEASE_ID=local
