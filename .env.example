# development | preview | production
HSEEHUB_ENV=development
NEXT_PUBLIC_HSEEHUB_ENV=development

# Preview must use an explicit origin; production must use a real HTTPS origin.
NEXT_PUBLIC_SITE_URL=

# Required for production builds and immutable release promotion.
HSEEHUB_RELEASE_ID=
RELEASE_VERSION=
ROLLBACK_VERSION=
